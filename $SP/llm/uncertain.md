- ★ max_tokens 전달 결함의 '실제 상한이 무엇인가'는 미확인. _opts_kw 에 max_tokens 가 없는 것은 코드로 확정했으나(llm.py:1250-1290), 그렇다면 SDK/모델 기본 상한이 무엇으로 걸리는지, 그리고 max_tokens 를 추가 전달했을 때 claude_code_sdk 의 ClaudeCodeOptions 가 그 파라미터를 받는지는 실측하지 않았다. 조치 전 1회 스모크 테스트 필요.
- ★ analyzer alias 7일 10.6M/285회의 내역 분해 실패. 호출자는 topic_pack._profile_batch(:145)와 evidence_pack._label_batch(:449) 둘뿐임을 grep 으로 확정했으나, 두 함수의 코드상 기대 빈도(팩 최대 4 + collect_all 3 ≈ 7회/일)와 실측 40~68회/일이 크게 어긋난다. source 컬럼이 둘 다 'daemon' 이라 구분 불가. 재시도·pick_slot_candidate 소진복구 재빌드·keyword_profile 경유 호출의 실제 발화를 로그로 확인해야 함. 이 alias 가 1위 소비원이라 분해 없이는 최대 절감 지렛대를 못 잡는다.
- run_sdk_query 경로(auto_repair run_auto_repair / run_auto_repair_targeted / delegate_to_claude_code)의 실제 토큰 소비량이 장부에 단 1행도 없다. 계측(_record_sdk_usage, claude_sdk_compat.py:200)이 2026-07-26 에 추가됐고 마지막 deep_audit 실행(07-26 03:25)이 계측 배포보다 앞선다. GUARDIAN 에서 가장 무거울 가능성이 큰 두 통로(max_turns=60/1200s, max_turns 무제한/600s)가 미계측 상태. 다음 토요일 2026-08-01 03:00 실행 후 재조회해야 확정.
- 위 실측치(analyzer 10.6M · writer 5.67M · guardian 3.41M · learn_eval 1.31M)는 대부분 2026-07-26 c3309fd 이전 값이다. 그 커밋이 invoke_text 경로에 disallowed_tools=['*'](31,468→227)와 scratch cwd(CLAUDE.md 48,940 차단)를 넣었고, 일일 총량이 07-26 4.11M → 07-27 2.00M → 07-28 1.91M 로 절반 이하가 됐다. 절감 판단을 옛 수치로만 하면 과대추정 위험. 다만 07-27/28 은 부분 관측 구간이라 세 변화(deep_audit 주1회 전환·alias 세분화·계측 추가)의 기여도 분리는 못 했다.
- guardian alias 0토큰 행 66/90 의 원인을 'timeout 120s 벽시계 도달 후 강제 포기'로 해석했다(근거: 행 간격 ~2분 · error_analyzer timeout=120 · llm.py hung/truncated 경로가 usage=None 으로 record_call). daemon.log 와 직접 대조하지는 못했다.
- writer_short_title 실측 18건/7일이 기대치(4/일)를 넘는 원인을 _PARAM_CACHE 실패 미저장 + _mandatory_thumbnail 3회 재시도 증폭으로 특정했으나, 2026-07-27 22:32~22:46 의 9건 연속 호출은 정규 발행 시각(07:00/21:00)이 아니어서 수동 실행·테스트일 가능성이 남는다. 그 9건을 제외한 정상 빈도는 미확정.
- j07_retry_pending 의 이론 상한(하루 144run × 20건)이 실측 10회/7일로 줄어드는 데 7겹 게이트 각각이 얼마나 기여하는지 분해하지 못했다. 최근 7일 error_log status 분포가 manual 786 / fixed 11 / ignored 7 / wontfix 1 로 status='new' 잔류가 0 이라 지금은 스윙이 집을 게 거의 없다. 오류가 몰리는 날엔 CB_MAX_HOUR=10 까지 올라가 하루 최대 240 세션이 이론상 가능 — 실측된 적은 없다.
- j06_design_learn 이 '2일 연속 LLM 산출물 채택 0'인 것은 07-27(결정론)·07-28(라이브러리) 두 날의 via 값으로 확인했다. 그 이전 기간의 채택률은 확인하지 않았다 — 만약 과거에 LLM 레시피가 정상 채택돼 왔다면 최근 2일이 예외일 수 있으므로, 잡 축소 전 design_recipes.json 의 via 분포 전수 확인을 권장.
- generate_content_angles 의 '소비자 0' 은 content_angles/extra_angles 를 읽는 파이썬·TS 코드가 0행인 것으로 확정했으나, trends JSON 을 직접 파싱하는 외부 소비자(대시보드 SWR /api/trends 응답을 프론트가 통째로 받아 쓰는 경우)까지는 추적하지 않았다. dashboard/lib/api.ts 의 RecommendItem 타입에 angle/hook/title 이 없는 것이 근거이나, 타입 밖 필드를 참조하는 코드가 있을 가능성은 남는다.
- law_enforcer.audit_factuality 의 LLM 레그가 도달 불가임은 확정했으나(호출자 2곳 모두 source_data 미전달), 이 감사를 '되살릴 것인가 폐기할 것인가'는 정책 판단이라 결정하지 않았다. prepublish_gate._combined_quality_call 과 검사 범위가 중복되는지 정밀 비교하지 않았다.
- cache_selfcheck() 실행 결과는 [C1] analyzer_chart(0.20배)·analyzer_evidence(0.00배) 두 건뿐이었다. 그러나 writer_long_body 는 cache=False 라 검사 대상 밖인데 cache_write 191,224 가 발생하고 있다([C2] 조건에 해당할 수 있음) — 검사가 이를 왜 안 잡았는지 확인하지 못했다.
- 주간 한도(seven_day) 가 총토큰 기준인지 출력 기준인지 모른다. API 응답의 limit_dollars/used_dollars 가 전부 null 이라 utilization 퍼센트만 있다. 총토큰 기준이면 데몬 기여 4.3%, 출력 기준이면 46% 로 '데몬 절감이 한도에 얼마나 기여하는가'의 결론이 정반대가 된다. 눈금 확인 전에는 절감 목표치를 세울 수 없다.
- JARVIS04_SCHEDULER/job_registry.py DEFAULT_JOBS 의 voice_index 잡이 참조하는 JARVIS03_RADAR.jobs.job_voice_index 함수가 존재하지 않는다(hasattr → False). 토큰과 무관하나 job_registry._resolve_callback 이 등록 시점 fail-fast 하도록 돼 있어 데몬 부팅에 영향 가능 — 별건으로 확인 필요.
