# 골든 테스트가 도는 데 필요한 최소 의존 — **CI 와 이 파일이 단일 진실 소스**
#
# ★ 왜 별도 파일인가 (2026-08-05)
#   CI 에 `pip install pytest python-dotenv` 라고 *적어 두었더니*, 테스트가 런타임
#   모듈을 하나 더 건드리는 순간 CI 가 red 가 됐다. 실측: 2026-08-04 커밋이
#   `post_quality_analyzer`(→ `import requests`)를 건드려 `2 failed, 89 passed`.
#   그런데 로컬은 초록이라 아무도 몰랐다 — **CI 가 '진짜 결함' 이 아니라 '빠진 의존'
#   으로 빨개지면, 사람은 CI 를 안 보게 된다.** 그게 게이트를 죽이는 가장 흔한 길이다.
#
# ★ 왜 requirements.txt 전체가 아닌가
#   280줄에 selenium·matplotlib·scipy 가 들어 있다. 테스트는 브라우저를 띄우지 않는다.
#   전부 설치하면 CI 가 느려지고, 느린 CI 는 꺼지는 CI 가 된다.
#
# ★ 규칙
#   테스트가 새 서드파티를 요구하면 **여기에 추가**한다. CI yaml 을 고치지 않는다.
#   `tests/test_publish_golden.py::test_CI가_테스트_의존을_이_파일에서_설치한다` 가
#   CI 와 이 파일이 갈라지는 것을 막는다.
pytest
python-dotenv
requests
# 밴딧(Linear UCB) 테스트가 행렬 연산을 실제로 돌린다 (2026-08-07)
numpy
# 심층감사 회차 판정 테스트가 **실제 스케줄러를 띄워** job_runs 행을 확인한다 (2026-08-08).
#  없으면 create_scheduler() 가 None 을 돌려주고 프로브가 죽는데, 그 죽음이
#  "행이 success='' 로 적혔다" 라는 *엉뚱한 진단* 으로 보고됐다 — ERRORS [587] 과 같은 병.
apscheduler
# 트렌드 판 보존 테스트가 JARVIS03_RADAR/collectors/google_collector 를 import 한다
# (2026-08-09). 그 모듈이 pandas + pytrends 를 쓴다 — apscheduler 와 같은 계열의 누락.
#   ★ 한 번에 다 넣지 못한 이유: pandas 만 넣고 재검하니 다음 줄의 pytrends 에서 또 멈췄다.
#     import 사슬은 **한 번에 하나씩** 드러난다 — 그래서 CI 를 기다리지 말고
#     `git archive HEAD` + 최소 의존 venv 로 로컬에서 끝까지 돌려봐야 한다.
pandas
pytrends

# ★ selenium (2026-08-10 추가) — 발행 모듈(`JARVIS08_PUBLISH/platforms/*_poster.py`)이 모듈 레벨에서
#   `selenium.webdriver` 를 쓴다. `tests/test_naver_session.py` 4건이 그 모듈을 *import 해서* 검사하므로
#   없으면 수집 단계에서 죽는다(CI 적색 4건). 발행 테스트가 발행 의존성을 요구하는 것은 정상 계층이다
#   — 수집 의존성이 데이터 모델 import 에 딸려오던 `feedparser` 건과는 성격이 다르다(그쪽은 지연 로드로 해소).
selenium
